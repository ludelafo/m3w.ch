name: whoami

services:
  whoami:
    container_name: ${WHOAMI_CONTAINER_NAME}
    hostname: ${WHOAMI_CONTAINER_NAME}
    image: traefik/whoami:${WHOAMI_IMAGE_VERSION}
    restart: unless-stopped
    ports:
      - 8080:80
