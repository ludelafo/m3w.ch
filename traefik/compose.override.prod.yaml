services:
  traefik:
    labels:
      ## Tinyauth
      - traefik.http.routers.${TRAEFIK_CONTAINER_NAME}.middlewares=tinyauth
      - tinyauth.apps.${TRAEFIK_CONTAINER_NAME}.config.domain=https://${TRAEFIK_FQDN}
      - tinyauth.apps.${TRAEFIK_CONTAINER_NAME}.ldap.groups=users
      ## Glance
      - glance.name=Traefik
      # https://github.com/glanceapp/glance/blob/main/docs/configuration.md#icons
      - glance.icon=sh:traefik
      - glance.url=https://${TRAEFIK_FQDN}
      - glance.description=Modern reverse proxy and load balancer.
      - glance.category=infrastructure
